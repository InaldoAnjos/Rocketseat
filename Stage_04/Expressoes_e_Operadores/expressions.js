/* Expressões e Operadores
    
    - Expressions;
    - Operators:
        - Binary;
        - Unary;
        - Ternary.
*/

let number = 1;

console.log(number + 1); //Binary
console.log(number++); // Unary
console.log(number > 1 ? `${number} É maior que 1` : `${number} Não é maior que 1`); // Ternary
